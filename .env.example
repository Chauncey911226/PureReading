# ============================================
# Pure Reading 环境变量模板（Vercel 版）
# ============================================
# 本地开发：
#   1. 复制本文件为 .env （注意去掉 .example 后缀）
#   2. 填入真实 API Key
#   3. 运行 node dev-local.js 启动本地后端
#
# 部署到 Vercel 时：
#   不需要上传 .env 文件，而是去 Vercel 控制台
#   → Project Settings → Environment Variables 手动添加
# ============================================

# 净网抽取服务 - Jina Reader
# 注册入口：https://jina.ai/  → 控制台领取 API Key
# 免费额度：注册后 10M tokens/月 + 500 RPM
# 不填也能用（约1000次/天匿名调用），但建议填上以解锁更高配额
JINA_API_KEY=

# LLM 降维翻译 - 硅基流动 Qwen/Qwen2.5-7B-Instruct
# 注册入口：https://siliconflow.cn/ → 控制台 → API 密钥
# 新用户送 2000 万 Token，9B 以下模型永久免费
# 必填
SILICONFLOW_API_KEY=

# ============================================
# 以下为可选配置
# ============================================

# 前端域名（用于 CORS 白名单）
# 部署到 Vercel 后填实际域名，例如 https://your-project.vercel.app
# 留空则允许所有来源（适合开发阶段）
ALLOWED_ORIGIN=
